# --- Stage 1: Core Mechanics (Zero Frameworks) ---
instructor     # Step 1.2: Forcing pristine structured outputs from LLMs
litellm     # Step 1.3: Unified translation layer across OpenAI/Anthropic/Gemini

pydantic      # Step 1.1 & 1.2: Raw tool definitions & structured schemas
langchain-openai
langchain-google-genai
langchain-community
langchain

# --- Stage 2: Vendor Toolkits (Single-Agent & Handoffs) ---
google-genai   # Step 2.1: Native Google SDK (2M context, prompt caching)
openai-agents # Step 2.2: OpenAI's multi-agent flat "Swarm" handoff framework
google-adk     # Step 2.3: Google's Agent Development Kit (Hierarchical trees & `adk web`)

# --- Infrastructure & Utilities ---
python-dotenv  # Safe local credential & API key management
httpx        # Underlying async HTTP client needed by raw call mechanics
streamlit
fastapi
uvicorn